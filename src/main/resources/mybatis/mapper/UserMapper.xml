<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="aladin.webhook.infra.mybatis.mapper.UserMapper">

    <!-- accountKey(external_user_key) 기준 사용자 조회 -->
    <select id="selectByAccountKey"
            resultType="aladin.webhook.infra.mybatis.model.UserRow">
        SELECT
            user_id,
            external_user_key,
            email,
            status,
            created_at,
            updated_at
        FROM users
        WHERE external_user_key = #{accountKey}
    </select>

    <!-- 이메일 변경 -->
    <update id="updateEmail">
        UPDATE users
        SET email = #{email}
        WHERE external_user_key = #{accountKey}
    </update>

    <!-- 상태 변경 (DELETED / APPLE_DELETED 등) -->
    <update id="updateStatus">
        UPDATE users
        SET status = #{status}
        WHERE external_user_key = #{accountKey}
    </update>

</mapper>
